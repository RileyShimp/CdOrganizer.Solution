@{
  Layout = "_Layout";
}

@using CdOrganizer.Models;

<h1>Genre.</h1>

@if (@Model.Count == 0)
{
  <h3>No genres have been added yet!</h3>
} 

@foreach (Genre genre in Model)
{
  <li>@Html.ActionLink($"{genre.Name}", "Details", new { id = genre.GenreId })</li>
}

<p>@Html.ActionLink("Pop that genre in there", "Create")</p>